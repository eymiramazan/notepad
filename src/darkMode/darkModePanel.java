package darkMode;

import javax.swing.*;
import java.awt.*;

// ConcreteProduct darkModePanel

public class darkModePanel extends JPanel {
    public darkModePanel() {
        setBackground(Color.DARK_GRAY);
    }
}
