package darkMode;

import javax.swing.*;
import java.awt.*;

// ConcreteProduct darkModeTextArea

public class darkModeTextArea extends JTextArea {
    public darkModeTextArea(){
        setBackground(Color.GRAY);
    }
}
