package blackMode;

import javax.swing.*;
import java.awt.*;

// ConcreteProduct blackModeMenu

public class blackModeMenu extends JMenu {
    public blackModeMenu(String name){
        super(name);
        setBackground(Color.BLACK);
        setForeground(Color.white);
    }
}
