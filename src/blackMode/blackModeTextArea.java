package blackMode;

import javax.swing.*;
import java.awt.*;

// ConcreteProduct blackModeTextArea

public class blackModeTextArea extends JTextArea {
    public blackModeTextArea(){
        setBackground(Color.DARK_GRAY);
        setForeground(Color.white);
    }
}
